# Gipformer - GPU / PyTorch dependencies (optional)
# Only needed when DEVICE=cuda (RTX 5060 Ti 16GB production, GTX 1650 Ti 4GB dev)
#
# Install:  pip install -r requirements.txt -r requirements-gpu.txt
#
# These packages are NOT on PyPI — they must be installed from the custom
# find-links indexes below. Do NOT include this file in a standard pip install.

--find-links https://k2-fsa.github.io/k2/cuda.html
--find-links https://csukuangfj.github.io/kaldifeat/cuda.html
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.8.0
torchaudio==2.8.0
k2==1.24.4.dev20250807+cuda12.8.torch2.8.0
kaldifeat==1.25.5.dev20250807+cuda12.8.torch2.8.0
sentencepiece==0.2.1

# icefall runtime dependencies
kaldialign
pypinyin==0.50.0
tensorboard
