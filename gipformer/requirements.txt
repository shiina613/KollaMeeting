# Gipformer - Vietnamese ASR Service
# https://huggingface.co/g-group-ai-lab/gipformer-65M-rnnt
#
# Install (CPU / dev):  pip install -r requirements.txt
# Install (GPU):        pip install -r requirements.txt -r requirements-gpu.txt

# ── FastAPI service ──────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0

# ── Redis client ─────────────────────────────────────────────────────────────
# fakeredis 2.x requires redis>=4.3; production uses redis 7.x
redis>=4.3.0

# ── Audio processing ─────────────────────────────────────────────────────────
scipy>=1.11.0
numpy>=1.20.0
soundfile>=0.12.0

# ── ASR / model (CPU ONNX fallback) ──────────────────────────────────────────
sherpa-onnx>=1.10.0
huggingface_hub>=0.20.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.0.0
hypothesis>=6.0.0
# sortedcontainers is a direct dependency of hypothesis (property-based testing)
sortedcontainers>=2.1.0
# fakeredis: in-memory Redis for unit/property tests (no real Redis needed)
fakeredis>=2.0.0
# pytest-asyncio: needed if async FastAPI route tests are added
pytest-asyncio>=0.21.0
